# @package _global_

defaults:
  - override /inference : mcmc
  - override /inference/sampler : sghmc
  - override /inference/sample_container: doubling

data:
  batch_size: 500

inference:
  sampler:
    lr: 2.e-7
  sample_container:
    max_items: 100
    
tag: sghmc