# @package _global_

defaults:
  - override /inference : mcmc
  - override /inference/sampler : sghmc_with_grad_est
  - override /inference/sampler/variance_estimator: interbatch
    
inference:
  sampler:
    lr: 5.e-5

tag: sghmc_var_est

