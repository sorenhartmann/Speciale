# @package _global_

defaults:
  - override /inference : vi

data:
  batch_size: 128

inference:
  n_particles: 4
  lr: 0.0001

trainer:
  gradient_clip_val: 10 
  gradient_clip_algorithm: "value"

tag: vi

