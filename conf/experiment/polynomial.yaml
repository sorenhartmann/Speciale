# @package _global_

defaults:
  - override /model : polynomial
  - override /data : sampled_data
  - override /inference : mcmc
  - override /sample_container : all
  - _self_

out_dir : polynomial

trainer:
  max_epochs: 100

inference:
  burn_in: 0