# @package _global_

defaults:
  - override /inference : mcmc
  - override /sample_container : fifo
  - override /sampler : sghmc

sampler:
  lr : 2.0e-06
inference:
  burn_in: 50

model:
  activation_func:
    _target_: torch.nn.
    




    

tag: sghmc