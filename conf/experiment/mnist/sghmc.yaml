# @package _global_

defaults:
  - override /inference : mcmc
  - override /inference/sampler : sghmc
  - override /inference/sample_container: all

data:
  batch_size: 500

inference:
  sampler:
    lr: 2.e-6
    resample_momentum_every: 200
    alpha: 0.02
    
    
tag: sghmc