# @package _global_

defaults:
  - override /inference : mcmc
  - override /sample_container : all
  - override /sampler : sghmc_with_grad_est
  - override /variance_estimator: inter_batch

sampler:
  lr : 2.0e-06

inference:
  burn_in: 50


tag: sghmc_interbatch