# @package _global_

defaults:
  - override /inference : mcmc
  - override /sample_container : fifo
  - override /sampler : sghmc_with_grad_est
  - override /variance_estimator: adam

sampler:
  lr : 2.0e-06

inference:
  burn_in: 50

tag: sghmc_adam