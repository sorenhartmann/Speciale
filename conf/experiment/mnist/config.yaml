# @package _global_
defaults:
  - /inference_config@_group_
  - group : sghmc_with_grad_est
  - override /hydra/sweeper: optuna
  - override /data : mnist
  - override /model : mlp_classifier
  - _self_
  
trainer:
  max_epochs: 800
  # progress_bar_refresh_rate: 0

hydra:
  sweeper:
    study_name: ${hydra.job.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
    storage: sqlite:///optuna.db

