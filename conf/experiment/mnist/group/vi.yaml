# @package _global_

defaults:
  - override /inference : vi

inference:
  n_samples: 10

hydra:
  sweeper:
    search_space:
      'lr': 
        type: float
        low: 1.e-8
        high: 1.e-4
        log: True
