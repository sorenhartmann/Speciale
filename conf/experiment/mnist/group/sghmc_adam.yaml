# @package _global_

defaults:
  - override /inference : mcmc
  - override /sample_container : all
  - override /sampler : sghmc_with_grad_est

sampler:
  variance_estimator:
    _target_: src.inference.mcmc.var_estimators.AdamEstimator
    adj_with_mean: True
  lr : 2.0e-06

inference:
  burn_in: 50