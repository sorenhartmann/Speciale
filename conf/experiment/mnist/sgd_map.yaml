# @package _global_

defaults:
  - override /inference : sgd

inference:
  lr : 1.e-03
  use_map : True
  prior_spec:
    default_prior:
      mixture_ratio: 0.25
      log_sigma_1: -2
      log_sigma_2: -6
  
data:
  batch_size: 128

tag: sgd-map

