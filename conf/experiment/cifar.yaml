# @package _global_
defaults:
  - override /inference: vi
  - override /data : cifar
  - override /model : conv_classifier
  - _self_
  
out_dir : cifar

inference:
  lr: 0.05

data:
  batch_size: 128

trainer:
  max_epochs: 800
  gradient_clip_val: 5.
  gradient_clip_algorithm: norm


# hydra:
#   sweeper:
#     study_name: ${hydra.job.name}/${now:%Y-%m-%d}/${now:%H-%M-%S}
#     storage: sqlite:///optuna.db

