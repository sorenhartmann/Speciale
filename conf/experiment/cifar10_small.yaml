# @package _global_
defaults:
  - /experiment/cifar10_small : "???"
  - override /data : cifar
  - override /model : conv_classifier
  - _self_
  
out_dir : cifar10_small

trainer:
  max_epochs: 800
  max_time: "00:01:30:00"

