# @package _global_

defaults:
  - override /inference/sampler: sghmc
  - _self_

inference:
  sampler:
    lr: 0.01
    beta : 0.02
    alpha : 0.3
    resample_momentum_every: 50
  steps_per_sample: 50 

distribution:
  grad_noise : 2.

legend: "SGHMC"