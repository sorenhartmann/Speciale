# @package _global_

defaults:
  - override /inference/sampler: sghmc_original_param
  - _self_

inference:
  sampler:
    B : 0.2
    M : 1
    C : 3

distribution:
  grad_noise : 2.

legend: "SGHMC"